import styled from "styled-components";
import { ReactComponent as GraphIcon } from "../../../assets/chart.svg";

const NoGraphIcon = styled(GraphIcon)`
  height: 50%;
  width: 50%;
  margin-bottom: 1em;
`;

export default NoGraphIcon;
